# P2-B1 experiment-environment records

Each preparation/run record must retain manifest hashes, encoder identifier and version, feature dimension, hardware, and UTC timestamp. P2-B1 contains only an environment-preparation record: no external images, features, checkpoints, trained classifiers, or benchmark metrics are present.
