# Institution internal API draft
Internal-only draft: `POST /cases`, `GET /cases/{id}`, `POST /cases/{id}/advance`, `GET /audit/{case_id}`, `POST /reports/{case_id}/sign`. No public routes are added in P3-B.
