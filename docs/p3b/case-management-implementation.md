# Case management implementation
Local in-memory `CaseRepository` implements the P3-A states and rejects report approval by the submitting analyst. It is test infrastructure, not institution storage.
