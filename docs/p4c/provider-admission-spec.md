# Provider admission

A Provider admission record binds Provider ID/version to approved model and calibration record hashes plus its declared scope. Authentication reports may carry all three hashes for later reproduction; this reference never changes an authenticity assessment.
