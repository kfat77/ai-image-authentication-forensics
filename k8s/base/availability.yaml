apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ai-photo-reconstructor
  namespace: ai-photo-reconstructor
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ai-photo-reconstructor
